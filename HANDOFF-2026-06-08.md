# Hand-off — `documentation/` bootstrap

_Generado por Strategos, 2026-06-08._

Este documento es la nota de traspaso. Léelo una vez; luego puedes archivarlo bajo `decisions/` cuando quieras.

---

## Qué se hizo

1. **Estructura completa de `documentation/`** según el brief (24 directorios).
2. **Gobernanza en español**:
   - `README.md` — mapa, reglas básicas, golden rule.
   - `CONTRIBUTING.md` — frontmatter obligatorio, estados, familias de versiones, workflow.
   - `roadmap.md` — completado / en curso / planificado / bloqueado / no haremos.
3. **Pipeline de ingestión** (`scripts/ingest.py`):
   - PDF / DOCX / PPTX / XLSX / MD / TXT (`.doc` se registra sin extraer).
   - SHA-256 → idempotencia.
   - Detección heurística de familia de versiones por *stem* normalizado.
   - Categoría inferida (`micromodulo-ia-salud`, `asignatura-telemedicina`, `curso-especialista-informatica-salud`, `administracion-*`, etc.).
   - Output: `content/<slug>.md` + entrada en `data/document-registry.yaml`.
4. **Corpus inicial ingerido**: los 37 ficheros bajo `raw_data/master-training/` están registrados.
   - 32 familias; 4 con varias versiones (ver `dashboard/families.md`).
   - 1 documento marcado canónico: **DOC-0035** (PDF maestro del Micromódulo).
5. **Esqueleto curricular** derivado del PDF canónico:
   - `curriculum/vision-general.md` — identidad, propósito, público objetivo, lo que falta confirmar.
   - `curriculum/estructura-modular.md` — tabla provisional de módulos.
6. **Templates** en `templates/`: `module.md`, `activity.md`, `assessment.md`, `decision.md`, `meeting.md`.
7. **Validador del registro** (`scripts/validate_registry.py`) — IDs únicos, ≤1 canónico por familia, recíprocos de `supersedes`/`superseded_by`, rutas que existen.
8. **Generador del dashboard** (`scripts/build_dashboard.py`) — `dashboard/README.md` + `dashboard/families.md`.
9. **Sitio MkDocs Material** (`website/mkdocs.yml` + `scripts/build_site.py`):
   - Tema en español, modo claro/oscuro, búsqueda built-in.
   - Construye en `website/site/` (en `.gitignore`).
   - `python scripts/build_site.py --serve` para servidor local.
10. **GitHub Actions** (`.github/workflows/validate-and-build.yml`):
    - Valida registro → regenera dashboard → build estricto del sitio → sube artefacto.
11. **Git inicializado** en rama `main`, commit inicial hecho, remote configurado a `https://github.com/jvinals/master-healthcare-ai.git`.

## Métricas actuales

```
documentos en registro:     37
categorías distintas:        7
familias de versiones:      32
familias con varias vers.:   4
canónicos confirmados:       1   (DOC-0035)
status=Draft:               36
status=Published:            1
```

## Lo que **tú** debes hacer ahora (orden)

### 1. Hacer el push inicial

El entorno donde se construyó esto no tiene credenciales de GitHub. Desde tu máquina:

```bash
cd documentation/
git push -u origin main
```

(o copia la carpeta `documentation/` a un repo limpio si prefieres). El remote ya está configurado.

### 2. Revisión humana del registro (1–2 horas, pero crítico)

Abre `data/document-registry.yaml` y, por cada entrada:

- **Confirma o corrige `family_id`.** El detector heurístico acertó en lo obvio pero falla en algunos casos. Ejemplos conocidos donde debería intervenir un humano:
  - `DOC-0026` (`Convenio EdgeHealth UCJC (firmado 2).pdf`) y `DOC-0027` (`Convenio EdgeHealth UCJC.pdf`) deberían compartir `family_id` — el detector los separó porque "2" sobrevivió al normalizador.
  - `DOC-0017` (Modelo Factura UCJC.docx) y `DOC-0020` (.doc) ya están agrupados — perfecto.
  - Familias del ANEXO I del Curso de Especialista (`DOC-0001`, `DOC-0029`, `DOC-0031`) deberían fusionarse manualmente.
- **Marca `canonical: true` en la versión vigente** de cada familia.
- **Cambia `status`** según realidad (`Approved`, `Published`, `Superseded`, `Archived`).
- **Rellena `author`** para cada documento.

Después corre:

```bash
python scripts/validate_registry.py
python scripts/build_dashboard.py
```

### 3. Decisiones retroactivas

Por cada cambio de versión histórico, crea un ADR en `decisions/` desde `templates/decision.md`. Aunque la decisión sea pasada, esto reconstruye la trazabilidad.

### 4. Extraer módulos del PDF canónico

`curriculum/estructura-modular.md` es un esqueleto. Cada módulo identificado en el PDF debería tener su propio archivo `modules/mod-XX-<slug>.md` desde `templates/module.md`.

### 5. (Opcional) Servir el sitio

```bash
pip install mkdocs-material
python scripts/build_site.py --serve     # http://127.0.0.1:8000
```

Cuando quieras publicar, decide el destino: GitHub Pages (workflow ya existe — sólo añade el step de deploy), Vercel/Netlify pointing al artefacto, o servidor propio.

## Decisiones explícitas que tomé

| Decisión | Por qué |
|---|---|
| Markdown + MkDocs en vez de Next.js + MDX | El brief pedía un sitio público; MkDocs Material lo entrega en horas, no en semanas, y el contenido sobrevive a cualquier migración futura. |
| Búsqueda built-in (Lunr) en vez de pgvector + OpenAI | Cero dependencias externas, cero coste, cero ops. Si la base de contenido crece y la búsqueda no basta, se añade un índice semántico ligero (sentence-transformers local) sin tocar nada más. |
| Registro YAML único en vez de SQLite/Postgres | YAML versionado en Git da diff legible, revisión humana, sin servidor. |
| `family_id` heurístico + revisión humana en vez de auto-resolución por embeddings | Más barato, más predecible, y mantiene al humano en el bucle de gobernanza — que es el punto del proyecto. |
| Sitio construido con symlinks vía `build_site.py` | MkDocs exige `docs_dir` como subdirectorio del config; con symlinks evitamos duplicar contenido y mantenemos las rutas semánticas (`curriculum/`, `dashboard/`, etc.). |
| `.doc` (legacy) registrado pero no extraído | No vale la pena meter LibreOffice headless en la cadena; son 2 ficheros. Si crecen, añadimos `libreoffice --headless --convert-to docx` en `extract`. |

## Lo que **no** hice (deliberadamente)

- **Next.js + Tailwind + MDX**: descartado por sobre-ingeniería (ver `roadmap.md`).
- **pgvector + OpenAI Embeddings**: igual.
- **Knowledge graph render**: `data/knowledge-graph.yaml` no se ha creado todavía; está en el roadmap. Se construirá automáticamente desde `related:` cuando empiece a haber relaciones que dibujar.
- **OCR**: ninguno de los ficheros actuales lo necesitaba.
- **Timeline visual**: pendiente; los datos están en el registro (`created`, `updated`) y se podrá generar cuando el registro tenga más fechas significativas.

## Si pasa esto, haz aquello

| Si… | Entonces… |
|---|---|
| Aparece un fichero nuevo en `raw_data/` | `python scripts/ingest.py` |
| Sospechas que el registro está corrupto | `python scripts/validate_registry.py` |
| Has cambiado mucho contenido | `python scripts/build_dashboard.py` |
| Quieres ver el sitio | `python scripts/build_site.py --serve` |
| Vas a fusionar dos familias | Edita `data/document-registry.yaml` (campo `family_id`) y corre el validador |
| Vas a aprobar una nueva versión canónica | (1) Crea `decisions/DEC-YYYY-XXX.md`, (2) cambia `canonical: true` en la nueva, `false` en la antigua, (3) actualiza `supersedes`/`superseded_by`, (4) mueve el fichero antiguo a `archive/`, (5) commitea |

---

**Recomendación final**: dedica 90 minutos esta semana a la revisión humana del registro (paso 2). Es la única tarea con la que el sistema deja de ser un esqueleto y empieza a ser memoria institucional real.
